import SetUpWrapper from '@components/SetUpWrapper'

const SettingsPage = () => {
  return (
    <SetUpWrapper>
      {/* Was used for account settings, we can use this for any future app settings */}
    </SetUpWrapper>
  )
}

export default SettingsPage
